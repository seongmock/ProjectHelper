# ProjectHelper 로드맵

> 이 로드맵은 앱 자체에도 데이터로 주입되어 있다 — 앱을 열면 자기 도구로 자기 로드맵을 간트 차트로 보여준다
> (스냅샷 "ProjectHelper Roadmap"으로도 저장되어 언제든 복원 가능).

## v1.0 — 기반 다지기 ✅ (2026-07-20 완료)

- [x] 동작 보존 리팩토링 (`taskTree.js` 추출, App.jsx −18%, 데드코드 −458줄)
- [x] 버그 수정 5건 (가져오기 왕복, 다크모드 설정, 캡처 배경, 설정 레이스, stale deps)
- [x] AI 연동: 작업 단위 REST API 13개 + OpenAPI + MCP 서버 12 도구
- [x] 동시성: 리비전 + If-Match(409) + 10초 폴링 — AI 쓰기가 열린 탭에 자동 반영
- [x] Playwright E2E 16개 · 스킬 3종 · 문서 3종 · 프로젝트 아이콘

## v1.1 — 품질 + 데모 + 핵심 기능 🚧 (진행 중)

- [x] **진행률(%)**: task별 0~100, 바 채움 오버레이 + 팝오버 슬라이더 + 표 배지, REST/MCP/HTML export 반영
- [x] **지연 하이라이트**: 종료일 경과 + 미완료 작업에 경고 엣지 (다크모드 대응)
- [x] **표↔타임라인 날짜 동기화**: 표의 날짜 편집이 timeRanges(첫 기간)를 수정 — 기존 desync 해소
- [x] **드래그 undo 정합성**: 드래그당 히스토리 1엔트리 (기존 3), Escape 취소 시 접힘 방치 버그 수정
- [x] **번들 분리**: html2canvas·htmlExporter 지연 로드
- [x] **CI**: GitHub Actions (build + E2E 21개) + README 배지
- [x] 셀프 데모: 로드맵을 앱 데이터로 주입

## v1.2 — 뷰 · 필터

- [ ] 라벨 필터 (labels 배열은 이미 데이터 모델에 존재 — UI만 필요)
- [ ] 정렬 옵션 (이름/시작일/진행률)
- [ ] 주별(weekly) 타임스케일 완성
- [x] **마일스톤 도형 렌더 통일** (2026-08-20) — `src/shared/milestoneShapes.js` 가 도형의 서술을 갖고 표·차트·내보내기가 표현만 각자 한다. 표의 별·깃발·삼각형이 벡터로 바뀌었다
- [ ] TableView 다중 기간 편집 UX (현재 첫 기간만 + "+N" 배지)

## v1.3 — 내보내기 · 공유

- [ ] PNG/PDF 파일 내보내기 (현재 클립보드/HTML만)
- [ ] 읽기전용 공유 링크 (서버 스냅샷 기반 `/share/:id`)
- [ ] 타임라인 날짜 윈도우 뷰/export 일치 (현재 ±14일 vs −14/+21일)
- [ ] 내보낸 HTML에도 지연 하이라이트 반영

## v1.5 — 다중 프로젝트 + 멀티유저 대비 ✅ (2026-07-22, v2.0 항목 조기 달성)

- [x] 프로젝트 1급 개념화: 레지스트리 + 프로젝트별 데이터/리비전/스냅샷 격리, 레거시 자동 마이그레이션
- [x] 스코프 API `/api/projects/{pid}/...` + default 별칭 하위호환
- [x] 헤더 ProjectSwitcher (생성/전환/이름변경/삭제), 전환 시 undo 히스토리 리셋·디바운스 flush
- [x] MCP `list-projects`/`create-project` + 전 도구 `projectId` 파라미터, AI 가이드 갱신
- [x] 멀티유저 대비: Caddy `X-Auth-User` 전달 → owner/createdBy 기록 (권한 강제는 실사용 시)

## v2.0 — 확장

- [x] ~~다중 프로젝트(워크스페이스)~~ → v1.5로 조기 달성
- [ ] 임계 경로(critical path) 계산·강조 — 의존성 그래프는 이미 range 레벨에 존재
- [ ] AI 일정 생성 통합 — 프롬프트 가이드 모달을 실제 생성으로 (MCP `add-task` 연쇄 호출)
- [ ] 실시간 협업 — 10초 폴링 → SSE 전환, 편집 커서 표시
- [ ] undo 히스토리 20 → 설정 가능 + 세션 간 유지

## 원칙

1. **동작 보존**: 모든 변경은 E2E 기준선 통과 후 병합 (CI 강제)
2. **AI 우선 설계**: 새 기능은 REST/MCP 표면도 함께 — 브라우저와 AI가 동등한 클라이언트
3. **단순성**: 도구 규모에 맞는 최소 구현 (프레임워크·네이티브 의존성 추가 없음 — 저장은 JSON 파일이 기본이고 SQLite 는 Node 내장 `node:sqlite` 로 선택 전환한다)
