# === Caddy 설정 파일 ===
# 도메인을 보유하고 있다면 아래 ':443'을 도메인 이름(예: example.com)으로 변경하세요.
# 그러면 Caddy가 자동으로 Let's Encrypt 인증서를 발급받아 HTTPS를 적용해줍니다.

:443 {
    # 도메인 없이 IP로 접속하거나 로컬 테스트용인 경우 'tls internal'을 사용합니다.
    # 주의: 이 경우 브라우저가 "안전하지 않음" 경고를 표시할 수 있지만, HTTPS 통신은 암호화됩니다.
    # 정식 도메인을 사용할 때는 아래 줄을 주석 처리하거나 삭제하세요.
    tls internal

    # 앱 컨테이너로 트래픽 전달
    reverse_proxy project-management-app:80
}
